const firebase = require('firebase/app');
require('firebase/auth');
require('firebase/firestore');

console.log('✅ Firebase SDK instalado corretamente!');
console.log('Versão do Firebase:', firebase.SDK_VERSION);